defaultTasks	"jar"

apply plugin: 'java'

jar {
    manifest {
        attributes 'Implementation-Title': 'Gradle Quickstart', 'Implementation-Version': 1.0 , 'Main-Class' : 'AntLabMain'
    }
}

dependencies {
    runtime files('lib/resources.jar')
}
